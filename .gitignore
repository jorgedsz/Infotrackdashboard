node_modules
dist
.DS_Store
*.local
.env

# Fuente cruda y datos sensibles
*.xlsx
*.csv
# Seed con data real del CRM — no se versiona (la data viene en vivo de GHL)
src/data/pipeline.json

# Logo original redundante (se usa public/logo-infotrack.png)
/Logo Infotrack Color  (1).png
